[phases.setup]
nixPkgs = ["jdk17"]

[phases.build]
cmds = ["./mvnw clean package -DskipTests -Dspring-boot.run.profiles=prod"]

[start]
cmd = "java -Xmx512m -Xms256m -jar target/construccion-backend-1.0.0.jar --spring.profiles.active=prod"
